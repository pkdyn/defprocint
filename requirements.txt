# defproc-monitor — scraper deps (classify.py is stdlib-only; dashboard is static).
# Pinned for reproducible CI/cron runs; bump deliberately after re-running the suite.
requests==2.34.2
beautifulsoup4==4.14.3
